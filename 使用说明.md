# Codex Tray 使用说明

Codex Tray 是一个 Windows 系统托盘工具，用于显示 Codex Cloud 的剩余额度、额度状态和重置时间。

## 1. 系统要求

- Windows 10 或 Windows 11，64 位
- 能够正常访问并登录 [ChatGPT](https://chatgpt.com/)
- 不需要安装 Python

程序文件为 `CodexTray.exe`。建议先将它放入固定目录，例如：

```text
D:\Tools\CodexTray\CodexTray.exe
```

确定位置后再启动，因为开机自启动会记录 EXE 的当前位置。

## 2. 首次启动

双击运行 `CodexTray.exe`。

程序不会显示普通窗口，而是在 Windows 任务栏右下角的通知区域显示图标。如果没有立即看到，请点击任务栏右下角的“显示隐藏的图标”箭头。

首次运行会默认开启“开机自动启动”。右键托盘图标，可以看到：

- **立刻刷新**：立即重新获取额度
- **打开 Codex analytics**：打开额度页面
- **导入/更新登录凭据…**：导入 ChatGPT 登录 Cookie
- **开机自动启动**：勾选或取消开机自启动
- **退出**：关闭程序

没有导入凭据时，程序会显示模拟额度，不能代表账号的真实使用情况。

## 3. 获取登录凭据

> 登录凭据包含 ChatGPT Cookie，等同于一段临时登录授权。不要发送给其他人，也不要上传到网盘、聊天群或代码仓库。

推荐使用已经登录 ChatGPT 的 Edge 或 Chrome 浏览器获取。

### 操作步骤

1. 在浏览器中打开并登录：

   <https://chatgpt.com/>

2. 打开 Codex Analytics 页面：

   <https://chatgpt.com/codex/cloud/settings/analytics>

3. 按 **F12** 打开开发者工具。

4. 切换到 **Network（网络）** 面板。

5. 在 Network 面板顶部的筛选框中输入：

   ```text
   analytics
   ```

6. 刷新当前页面。如果之前已经打开页面但列表为空，必须在 Network 面板保持打开的情况下刷新一次。

7. 在筛选结果中找到 URL 包含 `analytics` 的 ChatGPT 请求。通常是：

   ```text
   /codex/cloud/settings/analytics
   ```

8. 右键该请求，依次选择：

   ```text
   Copy → Copy as cURL (bash)
   ```

   必须选择 **Copy as cURL (bash)**，不要只复制 URL，也不要选择 `Copy request headers`。

## 4. 导入凭据

1. 右键 Codex Tray 托盘图标。
2. 选择 **导入/更新登录凭据…**。
3. 将刚才复制的完整 cURL 内容粘贴到文本框。
4. 如果目标机器访问 ChatGPT 需要代理，在代理框中填写，例如：

   ```text
   http://127.0.0.1:7890
   ```

   不需要代理时保持为空。

5. 点击 **保存**。
6. 右键托盘图标选择 **退出**，然后重新运行 `CodexTray.exe`。

凭据会保存在：

```text
%APPDATA%\CodexTray\credentials.bin
```

该文件使用 Windows DPAPI 加密，只能由保存它的 Windows 用户读取。因此迁移到另一台机器时，不能直接复制旧机器的 `credentials.bin`，需要在新机器重新获取并导入凭据。

## 5. 图标含义

图标中央数字表示 Codex 剩余额度百分比，外圈表示剩余进度：

- 绿色：剩余额度大于或等于 60%
- 黄色：剩余额度为 20%–59%
- 红色：剩余额度低于 20%
- 灰色问号：凭据失效、尚未导入凭据或请求发生错误

鼠标悬停在图标上，可以查看额度详情和预计重置时间。

## 6. 更新失效凭据

ChatGPT Cookie 可能因退出登录、修改密码、浏览器清理数据或服务端更新而失效。

如果图标变成灰色问号，或者提示登录已失效：

1. 确认浏览器仍然登录 ChatGPT。
2. 重新打开 Analytics 页面。
3. 按照“获取登录凭据”步骤重新复制 cURL。
4. 通过 **导入/更新登录凭据…** 覆盖旧凭据。
5. 退出并重新启动程序。

## 7. 开机自启动

程序首次运行时，会为当前 Windows 用户启用开机自启动，不需要管理员权限。

可以右键托盘图标，取消勾选 **开机自动启动**。如果之后重新勾选，程序会更新启动项，使其指向 EXE 当前所在位置。

如果移动了 `CodexTray.exe`：

1. 从新位置手动运行一次。
2. 确认托盘菜单中的 **开机自动启动** 已勾选。

## 8. 常见问题

### Network 中搜索不到 analytics

- 确认正在访问 Codex Analytics 页面。
- 确认 F12 已切换到 Network 面板。
- 清空筛选条件后刷新，确认 Network 能捕获其他请求。
- 再输入 `analytics` 进行筛选。
- 不要只查看 Fetch/XHR 类型；可以选择 **All（全部）**。

### 提示没有找到 Cookie

通常是复制方式不正确。请在 Network 请求上右键，选择：

```text
Copy → Copy as cURL (bash)
```

不要复制地址栏 URL，也不要只复制请求头。

### 导入后仍显示模拟额度

保存凭据后需要完全退出 Codex Tray，再重新运行。如果仍然无效，请重新获取一份最新 cURL。

### 无法连接或刷新超时

如果浏览器依赖代理访问 ChatGPT，Codex Tray 也必须使用同一个可用代理。重新导入凭据时填写代理地址。

### 任务栏没有图标

点击任务栏右下角的隐藏图标箭头。也可以在 Windows 的任务栏设置中，将 Codex Tray 设置为始终显示。

### 出现两个图标

可能同时运行了源码版本和 EXE 版本，或者重复启动了 EXE。分别右键旧图标选择 **退出**，只保留一个实例。

## 9. 迁移和卸载

### 迁移到其他机器

1. 将 `CodexTray.exe` 复制到新机器的固定目录。
2. 运行 EXE。
3. 在新机器浏览器中登录 ChatGPT。
4. 重新获取并导入 cURL 凭据。
5. 确认 **开机自动启动** 已勾选。

### 卸载

1. 右键托盘图标，取消勾选 **开机自动启动**。
2. 选择 **退出**。
3. 删除 `CodexTray.exe`。
4. 如需彻底清理，再删除：

   ```text
   %APPDATA%\CodexTray
   ```

删除该目录会同时删除本机保存的登录凭据和自启动偏好。
